# Core runtime
pydantic==2.9.2
pydantic-settings==2.5.2
PyYAML==6.0.2
python-dotenv==1.0.1
fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
jinja2==3.1.4
python-dateutil==2.9.0.post0

# LLM clients
openai>=1.99.9          # litellm >=1.82 uses openai.Omit, requires >=1.99
anthropic==0.39.0

# Observability
langfuse==2.53.4

# τ²-Bench harness (vendored at eval/tau2/) and its runtime deps. Harness adds
# eval/tau2/src to sys.path at import time; these are the transitive deps tau2
# pulls in. Keep aligned with eval/tau2/pyproject.toml.
litellm>=1.80.15,<1.82.7
loguru>=0.7.3
addict>=2.4.0
tenacity>=9.0.0
typer>=0.12.5
rich
tabulate>=0.9.0
docstring-parser>=0.16
deepdiff>=8.4.2
toml>=0.10.2
psutil>=7.0.0
pandas>=2.2.3

# Model Context Protocol (HubSpot MCP server client)
mcp>=1.0.0

# Scraping (policy-compliant)
playwright==1.48.0
beautifulsoup4==4.12.3

# JSON schema validation
jsonschema==4.23.0

# Statistical test for Act IV
numpy==2.1.3
scipy==1.14.1

# Dev / tests
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.3
